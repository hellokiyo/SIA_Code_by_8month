<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mapper-addrbook">

	<select id="getDB" resultType="lx.edu.springmvc.AddrBookVO">
		select 
			ab_id abId
		,  ab_name abName
		, ab_email abEmail
		, ab_comdept abComdept
		, ab_birth abBirth
		, ab_tel abTel
		, ab_memo abMemo
		from addrbook
		where ab_id =#{abId}
	</select>


	<!-- select -->
	<select id="getDBList" resultType="lx.edu.springmvc.AddrBookVO">
		select 
			ab_id abId
		,  ab_name abName
		, ab_email abEmail
		, ab_comdept abComdept
		, ab_birth abBirth
		, ab_tel abTel
		, ab_memo abMemo
		from addrbook
	</select>

	<insert id="insertDB">
		insert into addrbook(ab_name, ab_email, ab_comdept, ab_birth, ab_tel, ab_memo)
				values(#{abName}, #{abEmail}, #{abComdept}, #{abBirth}, #{abTel}, #{abMemo})
	</insert>
	
	<update id="updateDB">
		update set addrbook
			  ab_name = #{abName}
			, ab_email = #{abEmail}
			, ab_comdept = #{abComdept}
			, ab_birth = #{abBirth}
			, ab_tel = #{abTel}
			, ab_memo = #{abMemo}
	</update>
	
	<delete id="deleteDB">
		delete from addrbook 
				where ab_id = #{abId}
	</delete>
	
	
</mapper>