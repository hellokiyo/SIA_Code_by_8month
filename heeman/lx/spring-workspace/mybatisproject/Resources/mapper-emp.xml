<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mapper-emp">

	<select id="getEmpById" resultType="mybatis.Emp">
		select last_name lastName
		, first_name firstName
		, employee_id id
		from employees
		where employee_id = #{id}
	</select>

	<select id="getEmpListByDeptId" parameterType="int" resultType="mybatis.Emp">
	<!--id는 메서드 이름으로 봐도 좋다 / alas일수도 있고 /sql을 감싸는 이름으로볼 수도 있다.  -->
	<!-- parameterType이 int인 이유는 where에서 deptId를 찾고 있기때문에 deptId가 int형 이기 때문-->
		select last_name lastName
		, first_name firstName
		, employee_id id
		from employees
		where department_id = #{deptId}
	</select>


</mapper>